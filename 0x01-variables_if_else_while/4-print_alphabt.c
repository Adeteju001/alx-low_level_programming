#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry mode
 * Return: returns 0
 */
int main(void)
{
	char alphabt;
	for (alphabt = 'a'; alphabt <= 'z'; a++)
		if(alphabt == 'e' || alphabt == 'q')
			continue;
	putchar(alphabt);
	putchat('\n');
	return (0);
}
