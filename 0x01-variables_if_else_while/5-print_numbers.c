#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry mode
 * Return: returns 0
 */
int main(void)
{
	char num; 
	for (num = 0; num < 10; num++)
		printf("%d" , num"\n");
	return (0);
}
