#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry mode
 * Return: returns 0
 */
int main(void)
{
	char alpha

		for (alpha = 'z'; alpha >= 'a'; alpha--)
			putchar(alpha);
	putchar('\n');
	return (0);
}
